// Example in C++ using a graphics library
// Assuming there's a function or mode that sets how pixels are rendered

// Set rendering mode to original
renderer.setRenderMode(RenderMode::Original);

// Render an image using the original mode
renderer.renderImage(originalImage);
